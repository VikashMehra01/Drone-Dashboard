# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Python virtual environments
venv/
env/
ENV/
.venv/
.env

# Frontend dependencies
node_modules/
dist/
dist-ssr/
.npm/

# cv_pipeline runtime artifacts (per-drone logs/PID files created when a
# stream_processor.py subprocess is launched — regenerated on every run)
cv_pipeline/drone_*.log
cv_pipeline/drone_*.pid
cv_pipeline/drone_*.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Ensure model weights are NEVER ignored
!*.pth
!*.pt
!*.weights
